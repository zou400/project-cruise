# GitHub deployment — v0.10.3

## 安全な公開手順

1. 現在公開中のブランチから`cinematic-polish-v0.10.3`を作成します。
2. このフォルダの**中身すべて**をリポジトリの公開ルートへ配置します。
3. `index.html`、`runtime-config.js`、`assets/`の相対位置を変えないでください。
4. `node tests/validate-release.mjs`を実行します。
5. `Add Cinematic Polish v0.10.3`としてコミットします。
6. GitHub PagesのプレビューでデスクトップとiPhone Safariを確認します。
7. スモークテスト合格後に公開ブランチへマージします。

## 必須スモークテスト

- `320地点・501結果`と表示される
- 現在地・登録済み起点の両方が反応する
- 90分・2時間・半日の各モードで結果が出る
- 空港・湾岸橋・工業夜景・都市建築で画像が切り替わる
- `HANEDA INNOVATION CITY 足湯スカイデッキ`が横にはみ出さない
- ヒーロー、詳細、モバイル固定バーの全出発ボタンがGoogle Mapsを開く
- 2Dルート地図または安全な取得失敗表示が出る
- 再抽選、再抽選理由、帰還評価、不具合報告、学習ログ出力が動く
- AUTO / CINEMATIC / MIDNIGHT NOIRと文章濃度が切り替わる
- 3D未設定時に既存2D地図が消えない

## 3Dを有効にする場合

`docs/GOOGLE_MAPS_3D_SETUP.md`に従い、公開元を制限したブラウザ用キーを`runtime-config.js`へ設定します。未設定のまま公開しても他機能には影響しません。

## ロールバック

現在公開中のブランチを削除しないでください。不具合時はGitHub Pagesの公開元を直前の安定ブランチへ戻すか、このリリースの単一コミットをrevertします。
