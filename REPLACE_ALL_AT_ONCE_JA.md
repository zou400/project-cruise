# GitHubへ全件置換する手順（v0.11.0-rc5）

このパッケージは、部分追加ではなく**リポジトリ直下を一式置き換える前提**です。

1. ZIPをPCで展開する。
2. 展開後、`index.html`、`assets/`、`data/`、`.github/` が見える階層を開く。
3. その階層の中身をすべて選択する。
4. GitHubのリポジトリ直下へドラッグしてアップロードする。
5. 既存ファイルの置換を許可して `Commit changes` を押す。
6. GitHub Pagesのデプロイ完了後、ブラウザを強制再読込する。

## 重要

- ZIPファイルそのものはアップロードしない。
- 外側のフォルダを丸ごと一段下へ入れない。
- `assets/hero/precision/` にWebPが30枚あることを確認する。
- 公開後、URL末尾に `?heroPhase=night` を付けてHero表示を確認する。
- 古いキャッシュが残る場合は、プライベートブラウズまたはキャッシュ削除後に確認する。
